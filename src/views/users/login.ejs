<%- include('../partials/head',{styles:['login']}) %>

    <body>
        <%- include('../partials/header') %>
            
            <div class="overlay2" id="overlay2">
                <div class="formularioRegistroLogin" id="formularioRegistroLogin">
                    <form action="/login" method="POST" class="crear-formulario" enctype="application/json">
                        <h2>Ingresa tus datos</h2>
                        <div class="divNombreApellidologin">
                            <label class="labelCorreo">Correo:</label>
                            <input type="email" name="email" id="email" class="correoUsuario">
                            <div class="validator ">
                                <% if (locals.errors && errors.email) { %>
                                    <%= errors.email.msg %>
                                <% }%>
                            </div>
                        </div>
                        <div class="contrasenaLogin">
                            <label class="labelContraseña">Contraseña:</label>
                            <input type="password" name="password" id="password"
                                class="contraseñaUsuario" >
                        </div>
                        <p class="remember">Recordar contraseña</p>
                

                <div class="botonGuardarLogin">
                    <button type="submit" class="botonFormularioLogin">INGRESAR</button>
                    <button type="delete" class="botonFormularioLogin">CANCELAR</button>
                </div>
                </form>
            </div>
            </div>
            <%- include('../partials/footer') %>
    </body>

    </html>